complex datatype- used to represent complex nos 
immutable
x=a+bj
x.real, x.imag
cannot perform relational operators except(==,!=) gives TypeError
can perform arithmetic operations

zip() - combines 2 iterables value by value it also returns zip object
# iterators(obj) can be used only once 

any() and all() - build in func to evaluate truth conditions across an iterable
any() - like running or over a collection 
all() - like running and over a collection 